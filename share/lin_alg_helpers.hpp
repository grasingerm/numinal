#ifndef __LIN_ALG_HELPERS_HPP__
#define __LIN_ALG_HELPERS_HPP__

double norm_linf(const mat&, unsigned int&, unsigned int&);
double norm_linf(const mat&);
double norm_l2(const vec&);
//double norm_ln(const vec&, unsigned int);

#endif /* __LIN_ALG_HELPERS_HPP__ */
